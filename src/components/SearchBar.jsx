import React from "react";
import "./SearchBar.css";

const SearchBar = () => (
  <div className="search-bar">
    <input
      type="text"
      placeholder="Search..."
      className="search-input lato-regular"
    />
  </div>
);

export default SearchBar;
